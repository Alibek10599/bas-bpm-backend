syntax = "proto3";

package auth;

service AuthService {
  rpc Authenticate (Authenticate) returns (AuthenticateResult);
}

message Authenticate {
  string token = 1;
}

message AuthenticateResult {
  int32 userId = 1;
  string tenantId = 2;
  repeated string roles = 3;
}
